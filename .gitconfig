[color]
	ui = auto
[user]
	name = Oliver Schrenk
	email = oliver.schrenk@gmail.com
[core]
	excludesfile = ~/.global-gitignore
[alias]
	a = add
	au = add -u
	b = branch
	c = commit
	cam = commit --amend
    cv = commit -v
    cva = commit -v -a
	co = checkout
	d = diff
	dt = difftool
	ff = flow feature finish
	forget = update-index --assume-unchanged
	fs = flow feature start
	hf = flow hotfix start
	history = !git log --reverse --pretty=oneline | cut -c41- | nl
	hs = flow hotfix finish
	hub = !ghr $1
	ignore = !echo $1 >> .gitignore
	rf = flow release finish
	rs = flow release start
	s = status
	undo = checkout HEAD
	up = pull --rebase
	
[difftool "Kaleidoscope"]	
	cmd = ksdiff-wrapper git \"$LOCAL\" \"$REMOTE\"
[difftool]
	prompt = false
[diff]
	tool = Kaleidoscope
