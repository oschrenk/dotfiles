# Flush output immediately, even when piped.
--flush

# Like in Vim, ignore the case unless the pattern contains mixed case.
--smart-case

--type-add=markdown=.markdown,.md
--type-add=textile=.textile
--type-add=opencl=.cl