#
# List contents of directory, including hidden files, long format
#
function la --description "List contents of directory, including hidden files in directory using long format"
  ls -lah $argv
end
