{ ... }:

# Google Chrome preferences
{
  system.defaults.CustomUserPreferences = {
    "com.google.Chrome" = {
      # Use the system-native print preview dialog instead of Chrome's built-in one
      DisablePrintPreview = true;
    };
  };
}
