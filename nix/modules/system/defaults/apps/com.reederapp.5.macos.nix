{ ... }:

# Reeder preferences
{
  system.defaults.CustomUserPreferences = {
    "com.reederapp.5.macOS" = {
      # Font size XXL (default: smaller values)
      "article.font-size" = 19;

      # Content size category
      "app.content-size-category" = 5;
    };
  };
}
